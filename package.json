{
  "name": "Book-Review-App",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix book-review-frontend\" \"npm run start --prefix Backend\"",
    "install:all": "npm install && npm install --prefix book-review-frontend && npm install --prefix Backend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
